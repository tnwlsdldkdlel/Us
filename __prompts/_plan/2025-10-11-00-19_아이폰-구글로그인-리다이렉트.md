# 아이폰 구글 로그인 리다이렉트 수정

## Todo
- [x] iOS Info.plist에 Supabase OAuth 딥링크 스킴 추가
- [ ] Supabase Authentication 설정에서 `us://login-callback` 허용 여부 확인 (문서 안내)
- [x] 수정 사항 문서화 및 사용자 안내

## Plan
1. 현재 Info.plist의 URL 스킴 구성을 확인한다.
2. `us://login-callback`을 처리할 수 있도록 URL 타입을 추가한다.
3. Supabase 콘솔에서 redirect URL 등록 필요성을 안내한다.
4. 작업 로그 업데이트 후 결과 요약한다.

## Working
- Info.plist에 `CFBundleURLTypes` 항목을 확장해 `us` 스킴 등록
- Supabase 콘솔에서 Additional Redirect URL로 `us://login-callback` 추가 필요성 명시

## Result
- Info.plist 수정 완료(딥링크 스킴 추가), Supabase 설정 업데이트 후 로그인 리다이렉트 문제 해결 가능
